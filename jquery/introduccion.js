$(document).ready(function(){
    
    /* 
        $ - Indicamos que vamos a acceder a jQuery.
        selector - Accedemos al elemento que queramos trabajar.
        accion - Lo que querermos hacer con el elemento seleccionado.
    */
    
    $(selector).accion();


});