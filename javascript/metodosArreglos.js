'use strict'

let amigos = ['Carlos', 'Alejandro', 'Cesar', 'Manuel', 'Luis'];

// Método length
// document.write(amigos.length);

// Método join
    // let cadenaTexto = '**'
    // document.write(amigos.join(cadenaTexto));

// Método pop y push
    // amigos.pop();
    // amigos.push('Felipe');
    // console.log(amigos)

// Método shift(elimina el primer elemento) y unshift(poner un elemento al principio del arreglo)
    // amigos.shift();
    // amigos.unshift('Oscar');
    // console.log(amigos);

// Método concat
    // let amigos2 = ['Felipe', 'Oscar'];
    // let todosMisAmigos = amigos.concat(amigos2);
    // console.log(todosMisAmigos);

// Método sort y reverse - Ordenamiento de cadenas de texto
    amigos.sort();
    amigos.reverse();
    console.log(amigos);
